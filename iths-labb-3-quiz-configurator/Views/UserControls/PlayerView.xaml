<UserControl x:Class="iths_labb_3_quiz_configurator.Views.UserControls.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:iths_labb_3_quiz_configurator.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid ShowGridLines="False">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="50" />
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="50" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="25" />
            <RowDefinition Height="100" />
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Margin"
                        Value="10" />
                <Setter Property="HorizontalAlignment"
                        Value="Center" />
                <Setter Property="VerticalAlignment"
                        Value="Bottom" />
            </Style>
            <Style TargetType="Label">
                <Setter Property="FontWeight"
                        Value="Bold" />
            </Style>
            <Style TargetType="Button">
                <Setter Property="OverridesDefaultStyle"
                        Value="True" />
                <Setter Property="Background"
                        Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
                <Setter Property="Margin"
                        Value="10" />
                <Setter Property="BorderBrush"
                        Value="Black" />
                <Setter Property="Foreground"
                        Value="Black" />
                <Setter Property="BorderThickness"
                        Value="0.5" />
                <Setter Property="FontSize"
                        Value="16" />
                <Setter Property="FontWeight"
                        Value="Normal" />

                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background"
                                Value="{Binding Background, RelativeSource={RelativeSource self}}" />
                    </Trigger>
                </Style.Triggers>
            </Style>

        </Grid.Resources>
        
        <TextBlock Grid.Column="2"
                   Grid.ColumnSpan="2"
                   Grid.Row="1"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Bottom"
                   FontSize="16"
                   TextWrapping="Wrap"
                   Text="{Binding Query}" />
        
        <Label Grid.Column="2"
               Grid.Row="2"
               Grid.ColumnSpan="2"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Content="{Binding QuestionOfTotal}" />
        
        <Label Grid.Column="4"
               Grid.Row="1"
               FontSize="16"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Content="{Binding CurrentTime}" />
        
        <Button Grid.Column="2"
                Grid.Row="3"
                Content="{Binding Alt1.Alt}"
                Background="{Binding Alt1.Bg}"
                Command="{Binding MakeAGuessCommand}"
                CommandParameter="{Binding Alt1}"/>
        
        <Button Grid.Column="3"
                Grid.Row="3"
                Content="{Binding Alt2.Alt}"
                Background="{Binding Alt2.Bg}"
                Command="{Binding MakeAGuessCommand}"
                CommandParameter="{Binding Alt2}"/>
        
        <Button Grid.Column="2"
                Grid.Row="4"
                Content="{Binding Alt3.Alt}"
                Background="{Binding Alt3.Bg}"
                Command="{Binding MakeAGuessCommand}"
                CommandParameter="{Binding Alt3}"/>

        <Button Grid.Column="3"
                Grid.Row="4"
                Content="{Binding Alt4.Alt}"
                Background="{Binding Alt4.Bg}"
                Command="{Binding MakeAGuessCommand}"
                CommandParameter="{Binding Alt4}" />
    </Grid>
</UserControl>
